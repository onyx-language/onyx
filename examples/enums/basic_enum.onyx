enum StructLikeKinda {
    A(x: i32),
    B(y: [u8]),
    C(z: [char])
}

enum Untyped {
    A,
    B,
}

enum ThisIsHereBecauseIDontHaveVariableDeclarations<T, U> {
    A(a: raw u8),
    B(a: weak Untyped<T, U>),
}